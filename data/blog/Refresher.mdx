---
title:  A Quick Python  refresher for dummies
date: '2024-08-29'
tags: ['Python']
draft: false
summary: A comprehensive guide to refresh your python skills, covering basics, data types, variables, operators, control structures, arrays, methods, OOP concepts, exception handling.
---


##  A Quick Python refresher

Here are some basic notes on Python for beginners:

### Python Basics

#### 1. **Introduction to Python**
- **Python** is a high-level, interpreted programming language known for its readability and simplicity.
- Created by **Guido van Rossum** and first released in 1991.

#### 2. **Setting Up Python**
- Download and install Python from the official website: [python.org](https://www.python.org/).
- Use an Integrated Development Environment (IDE) like **PyCharm**, **VS Code**, or **Jupyter Notebook**.

#### 3. **Basic Syntax**
- **Variables**: Used to store data. No need to declare the type.
  ```python
  x = 5
  name = "Alice"
  ```
- **Data Types**: Common types include `int`, `float`, `str`, `list`, `tuple`, `dict`, and `bool`.
  ```python
  age = 25          # int
  height = 5.9      # float
  name = "Alice"    # str
  is_student = True # bool
  ```

#### 4. **Control Flow**
- **If Statements**: Used for conditional execution.
  ```python
  if age > 18:
      print("Adult")
  else:
      print("Minor")
  ```
- **Loops**: `for` and `while` loops for iteration.
  ```python
  for i in range(5):
      print(i)
  
  while age < 30:
      age += 1
  ```

#### 5. **Functions**
- Defined using the `def` keyword.
  ```python
  def greet(name):
      return f"Hello, {name}!"
  
  print(greet("Alice"))
  ```

#### 6. **Lists and Dictionaries**
- **Lists**: Ordered, mutable collections.
  ```python
  fruits = ["apple", "banana", "cherry"]
  print(fruits[0])  # Output: apple
  ```
- **Dictionaries**: Key-value pairs, unordered.
  ```python
  student = {"name": "Alice", "age": 25}
  print(student["name"])  # Output: Alice
  ```

#### 7. **Modules and Libraries**
- Importing modules using `import` keyword.
  ```python
  import math
  print(math.sqrt(16))  # Output: 4.0
  ```

#### 8. **File Handling**
- Reading and writing files.
  ```python
  with open("file.txt", "r") as file:
      content = file.read()
  
  with open("file.txt", "w") as file:
      file.write("Hello, World!")
  ```

#### 9. **Error Handling**
- Using `try` and `except` blocks.
  ```python
  try:
      result = 10 / 0
  except ZeroDivisionError:
      print("Cannot divide by zero")
  ```

#### 10. **Basic Libraries**
- **NumPy**: For numerical operations.
- **Pandas**: For data manipulation and analysis.
- **Matplotlib**: For plotting and visualization.

Feel free to ask (getharisree@outlook.com) if you need more details on any specific topic! Happy coding! 😊
    

